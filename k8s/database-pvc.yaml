apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: database-pvc
  namespace: hireflow

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: local-path

  resources:
    requests:
      storage: 5Gi